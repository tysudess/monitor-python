name: Release v0.0.6 UI Fixes

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  manual-only:
    runs-on: ubuntu-latest
    steps:
      - name: Publication disabled during UI development
        run: |
          echo "Automatic v0.0.6 publication is disabled."
          echo "Use an explicit release workflow when the UI is approved."
