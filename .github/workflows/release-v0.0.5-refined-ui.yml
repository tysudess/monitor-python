name: Release v0.0.5 Refined UI (manual legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-release-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain legacy workflow status
        run: |
          echo "Automatic v0.0.5 publishing is disabled in monitor-python."
          echo "Use the normal validation workflow while the new layout is under development."
          echo "Rebuild the release pipeline explicitly when a new version is ready to publish."
